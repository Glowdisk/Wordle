    plugins {
        id 'java'
        id 'application'
    }

    group = 'com.example'
    version = '1.0-SNAPSHOT'

    repositories {
        mavenCentral() // Standard repository
    }

    dependencies {
        // You don't need to add anything for Swing since it's built-in
    }

    application {
        mainClass = 'Wordle' // Make sure this points to your main class (update if needed)
    }

    jar {
        manifest {
            attributes(
                    'Main-Class': 'Wordle'
            )
        }
    }